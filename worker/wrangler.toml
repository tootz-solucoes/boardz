name = "clickup-proxy"
main = "index.js"
compatibility_date = "2024-01-01"

# CLICKUP_TOKEN é configurado como secret (nunca vai pro git):
# pnpm wrangler secret put CLICKUP_TOKEN --config worker/wrangler.toml
